language: node_js
node_js:
  - lts/*
install:
  - yarn install # npm ci
script:
  - yarn vuepress build # npm run docs:build
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: docs/.vuepress/dist
  github_token: 125c878641b183fe4d2d76e36cbb8e5ccaf7e01d
  keep_history: true
  on:
    branch: main