language: node_js
node_js:
  - lts/*
install:
  - yarn install # npm ci
script:
  - yarn vuepress build # npm run docs:build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  on: master