language: node_js
node_js:
  - lts/*
install:
  - yarn install # npm ci
script:
  - yarn vuepress build # npm run docs:build
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: docs/.vuepress/dist
  github_token: 160848bb16d90088582bff39a5eb7386668d2ab8
  keep_history: true
  on:
    branch: master